// The showcase is now a routed documentation site — one page per
// component with props/events tables and example galleries.
// See src/lib/docs/. Mount via /#showcase (see main.tsx).
export { default } from './docs/DocsApp'
